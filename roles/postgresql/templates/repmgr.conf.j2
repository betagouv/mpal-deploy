cluster={{ repmgr_cluster_name }}
node={{ repmgr_node }}
node_name={{ inventory_hostname }}
use_replication_slots=1
conninfo='host={{ ansible_ssh_host }} user=repmgr dbname=repmgr'
pg_bindir=/usr/lib/postgresql/9.6/bin
